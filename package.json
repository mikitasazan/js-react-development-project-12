{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Hexlet Chat — a simplified version of Slack.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --dir frontend run build",
    "lint": "pnpm --dir frontend run lint",
    "start": "start-server -s ./frontend/dist"
  },
  "dependencies": {
    "@hexlet/chat-server": "^2.0.4"
  }
}
